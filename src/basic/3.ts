// Зміна з обєднаним типо
let variable: string | number;
variable = "hello";
console.log(variable);
variable = 124;
console.log(variable);

// Зміна з літеральним типом
let state: "enabled" | "disabled";
state = "enabled";
console.log(state);
